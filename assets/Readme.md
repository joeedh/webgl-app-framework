To compile shape files, run `node compile_shapes.js`
To render iconsheets, run `python render_icons.py`

When adding new icons to the sheet, make sure update
scripts/editors/icon_enum.js.  Just add to the end
of the enum.
 
