## Sculptcore toolmode:

[ ]: Add icon for feature align brush
[ ]: drawWireframe wireframe isn't updated during sculpt strokes (or at all)

## Box modeling toolmode
[ ]: circle select should draw a brush circle.
[ ]: select all/none should apply to all element domains not just the active selection mode.
[ ]: Loopcut should show an interactive preview.
[ ]: Extrude individual faces crashes.
[ ]: boxmodel's drawPoints option should only apply if vertex selection mode is enabled.
[ ]: the element under the mouse should be highlighted (respecting selection modes) when
     the user hovers over them.
[ ]: implement left click selection (select closest element in enabled selection mode 
     to mouse cursor).
[ ]: implement edge loop select (invoked by holding CTRL and selecting an edge)
[ ]: implement face loop select (invoked by holding CTRL and selecting a face)
[ ]: implement face loop edge select (selects all the edges in between the faces of
     a face loop, invoke with ctrl-shift-select-edge)
[ ]: Extrude wire crashes
[ ]: Inset faces crashes
[ ]: Extrude region crashes
[ ]: Pattern-based subdivide is either not being invoked by 
     box modelling toolmode's subdivide op or just doesn't work.
