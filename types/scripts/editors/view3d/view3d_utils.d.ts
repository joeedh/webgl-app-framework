export function project(co: any, rendermat: any, viewSize: any): any;
export function calcUpdateHash(view3d: any, do_objects?: boolean): number;
export namespace CursorModes {
    let TRANSFORM_CENTER: number;
    let DEPTH_TEST: number;
    let LOCKED: number;
}
export namespace OrbitTargetModes {
    let FIXED: number;
    let CURSOR: number;
}
