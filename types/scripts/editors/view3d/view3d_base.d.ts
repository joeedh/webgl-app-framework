export namespace View3DFlags {
    let SHOW_CURSOR: number;
    let SHOW_RENDER: number;
    let ONLY_RENDER: number;
    let LOCAL_CURSOR: number;
    let SHOW_GRID: number;
    let SHOW_CAMERA_VIEW: number;
    let USE_CTX_CAMERA: number;
}
export namespace CameraModes {
    let PERSPECTIVE: number;
    let ORTHOGRAPHIC: number;
}
